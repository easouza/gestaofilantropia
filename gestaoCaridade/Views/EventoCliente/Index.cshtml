@model IEnumerable<gestaoCaridade.Models.EventoCliente>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ClienteSelecionado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EventoSelecionado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ClienteSelecionado.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EventoSelecionado.Nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEventoCliente">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEventoCliente">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEventoCliente">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
